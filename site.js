const b=document.getElementById('menuBtn'),n=document.getElementById('mainnav');if(b&&n)b.addEventListener('click',()=>{n.classList.toggle('open');b.textContent=n.classList.contains('open')?'✕':'☰'});const f=document.getElementById('contactForm'),fb=document.getElementById('formFeedback');if(f)f.addEventListener('submit',async e=>{e.preventDefault();const btn=f.querySelector('button');btn.disabled=true;btn.textContent='Se trimite...';fb.textContent='';try{const r=await fetch('https://formsubmit.co/ajax/office@ecaro.ro',{method:'POST',headers:{Accept:'application/json'},body:new FormData(f)});const j=await r.json();if(!r.ok||j.success===false||j.success==='false')throw new Error(j.message||'error');f.reset();fb.className='form-feedback success';fb.textContent='✓ Solicitarea a fost trimisă. Te contactăm în cel mai scurt timp.'}catch(err){fb.className='form-feedback error';fb.textContent='Trimiterea nu a reușit. Scrie-ne la office@ecaro.ro.'}finally{btn.disabled=false;btn.textContent='Trimite solicitarea →'}});